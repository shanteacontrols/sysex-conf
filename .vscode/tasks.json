{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "make",
            "type": "shell",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "coverage",
            "command": "./Tests.out && lcov --directory . --capture --output-file coverage.info && lcov --remove coverage.info 'tests/*' 'usr/*' '*gtest*' '*googletest*' --output-file coverage.info && lcov --list coverage.info",
            "type": "shell",
            "problemMatcher": []
        }
    ]
}